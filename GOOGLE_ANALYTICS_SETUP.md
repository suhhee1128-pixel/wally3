# Google Analytics 설정 가이드

이 프로젝트는 **Google Analytics 4 (GA4)**와 **Vercel Analytics**를 함께 사용합니다.

## 📊 두 가지 분석 도구의 차이점

### Vercel Analytics
- **목적**: 배포 성능, 속도, 함수 실행 시간 등 인프라 분석
- **자동 설정**: Vercel 배포 시 자동으로 활성화됨
- **데이터**: 페이지 로드 시간, Core Web Vitals, Edge Function 실행 시간 등

### Google Analytics 4
- **목적**: 사용자 행동, 전환 추적, 세그먼트 분석 등 비즈니스 분석
- **수동 설정**: Google Analytics 계정 생성 및 측정 ID 필요
- **데이터**: 페이지뷰, 이벤트, 사용자 세션, 전환율 등

## 🚀 Google Analytics 설정 방법

### 1단계: Google Analytics 계정 생성

1. [Google Analytics](https://analytics.google.com/)에 접속
2. **측정 시작** 클릭
3. 계정 이름 입력 (예: "Wally3")
4. 속성 이름 입력 (예: "Wally3 App")
5. 보고 시간대 및 통화 선택
6. 비즈니스 정보 입력 (선택사항)

### 2단계: 데이터 스트림 생성

1. **웹** 플랫폼 선택
2. 웹사이트 URL 입력:
   - 로컬: `http://localhost:3000`
   - 배포: `https://your-vercel-app.vercel.app`
3. 스트림 이름 입력 (예: "Wally3 Web")
4. **만들기** 클릭

### 3단계: 측정 ID 복사

1. 생성된 데이터 스트림에서 **측정 ID** 복사
   - 형식: `G-XXXXXXXXXX` (예: `G-ABC123XYZ`)
2. 이 ID를 `.env` 파일에 추가해야 합니다

### 4단계: 환경 변수 설정

#### 로컬 개발 환경

`.env` 파일에 다음을 추가:

```bash
# Google Analytics 측정 ID
REACT_APP_GA_MEASUREMENT_ID=G-XXXXXXXXXX
```

**중요**: `G-`로 시작하는 측정 ID를 그대로 입력하세요.

#### Vercel 배포 환경

1. Vercel Dashboard → 프로젝트 선택
2. **Settings** → **Environment Variables**
3. 다음 환경 변수 추가:
   - **Name**: `REACT_APP_GA_MEASUREMENT_ID`
   - **Value**: `G-XXXXXXXXXX` (Google Analytics에서 복사한 측정 ID)
   - **Environment**: Production, Preview, Development 모두 선택
4. **Save** 클릭
5. **Redeploy** 실행 (환경 변수 변경 후 재배포 필요)

## 📈 추적되는 이벤트

현재 다음 이벤트들이 자동으로 추적됩니다:

### 페이지뷰
- `/spending` - 지출 페이지
- `/chat` - AI 채팅 페이지
- `/analytics` - 분석 페이지
- `/mood` - 기분 페이지
- `/profile` - 프로필 페이지
- `/daily-spending` - 일일 지출 페이지

### 사용자 액션
- `login` - 로그인
- `logout` - 로그아웃
- `transaction_added` - 거래 추가 (지출만 추적)
  - `value`: 거래 금액
  - `category`: 카테고리
- `ai_chat` - AI 채팅
  - `ai_character`: AI 캐릭터 ID (`catty` 또는 `futureme`)
  - `message_length`: 메시지 길이

## 🔍 Google Analytics에서 데이터 확인

1. [Google Analytics](https://analytics.google.com/) 접속
2. 대시보드에서 다음을 확인할 수 있습니다:
   - **실시간**: 현재 활성 사용자 수
   - **보고서** → **참여도** → **페이지 및 화면**: 페이지뷰 통계
   - **보고서** → **참여도** → **이벤트**: 이벤트 통계
   - **보고서** → **참여도** → **전환**: 전환 이벤트

## 🛠️ 개발 모드에서 테스트

개발 모드(`npm start`)에서는 Google Analytics가 **테스트 모드**로 실행됩니다:
- 이벤트는 실제로 전송되지만 개발 환경임을 표시합니다
- 콘솔에 추적 로그가 출력됩니다

## ⚠️ 주의사항

1. **개인정보 보호**: Google Analytics는 사용자 데이터를 수집합니다. 개인정보 보호 정책에 따라 사용자에게 알려야 할 수 있습니다.

2. **GDPR 준수**: 유럽 사용자의 경우 GDPR 동의를 받아야 할 수 있습니다.

3. **측정 ID 보안**: 측정 ID는 공개되어도 안전하지만, 민감한 정보는 아닙니다.

4. **데이터 지연**: Google Analytics 데이터는 실시간이 아닐 수 있으며, 최대 24-48시간 지연될 수 있습니다.

## 📝 추가 이벤트 추적하기

새로운 이벤트를 추적하려면 `src/lib/analytics.js`에 함수를 추가하고, 컴포넌트에서 호출하세요:

```javascript
// src/lib/analytics.js에 추가
export const trackCustomEvent = (eventName, params) => {
  trackEvent(eventName, params);
};

// 컴포넌트에서 사용
import { trackCustomEvent } from '../lib/analytics';

const handleAction = () => {
  trackCustomEvent('button_click', {
    button_name: 'save_profile',
    page: 'profile'
  });
};
```

## 🔗 참고 자료

- [Google Analytics 4 문서](https://developers.google.com/analytics/devguides/collection/ga4)
- [react-ga4 문서](https://github.com/codler/react-ga4)
- [Vercel Analytics 문서](https://vercel.com/docs/analytics)

